{
  "config": {
    "step": {
      "user": {
        "title": "Home Energy Setup",
        "description": "Set a 4-digit passcode for settings access in the Home Energy dashboard.",
        "data": {
          "settings_passcode": "Settings Passcode (4 digits)"
        }
      }
    },
    "error": {
      "invalid_passcode": "Passcode must be exactly 4 digits"
    }
  }
}
